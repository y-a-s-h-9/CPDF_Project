//now let's add the color that we will need to project
import 'package:flutter/material.dart';

class MainColor {
  static Color primaryColor = Color.fromARGB(255, 255, 250, 235);
  static Color secondaryColor = Color.fromARGB(255, 255, 138, 117);
  static Color accentColor = Color.fromARGB(255, 255, 225, 52);
}
